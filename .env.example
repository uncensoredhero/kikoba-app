# PostgreSQL connection string. Set real credentials in local or deployment environment variables.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/kikoba?schema=public"

NODE_ENV="development"
PORT="3000"

# Authentication secrets will be added in the authentication phase.
